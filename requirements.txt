# MACD 量化交易系统依赖
# 兼容 Python 3.6+

# 核心依赖
pandas>=1.0.0,<1.2.0
numpy>=1.18.0,<1.20.0

# 数据源（tushare 兼容 Python 3.6）
tushare>=1.2.60

# 可选数据源：akshare 需要 Python 3.7+，如需使用请手动安装
# pip install akshare  # Python 3.7+ only

# 技术指标（可选，需要系统安装 TA-Lib C 库）
# ta-lib>=0.4.19

# 命令行
click>=7.0.0
rich>=9.0.0

# 配置管理
pyyaml>=5.3.0
python-dotenv>=0.15.0

# 日志
loguru>=0.5.0

# 数据库
# 使用 1.3.x 版本避免依赖 greenlet（greenlet 需要编译 C 扩展）
sqlalchemy>=1.3.0,<1.4.0

# 开发依赖（可选）
# pytest>=6.0.0
# pytest-cov>=2.12.0
# black>=20.8b1
# isort>=5.6.0
# mypy>=0.800
